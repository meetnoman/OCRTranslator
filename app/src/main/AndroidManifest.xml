<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.translator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LiveTextTranslation"
            android:theme="@style/AppNoTheme" />
        <activity android:name=".TextTranslation" />
        <activity android:name=".historyActivity" />
        <activity android:name=".TextExtracted" />
        <activity android:name=".ImageViewActivity" />
        <activity
            android:name=".CameraViewActivity"
            android:theme="@style/AppNoTheme" />
        <activity android:name=".LanguageOutputActivity" />
        <activity android:name=".LanguageInputActivity" />
        <activity android:name=".TextAugmenter">

        </activity>
    </application>

</manifest>